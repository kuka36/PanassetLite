---
description: LLM 接口请求规范：本地模型走 Vite 代理避免 CORS
globs: "src/services/**/*.ts"
alwaysApply: false
---

# LLM 请求规范

浏览器直连本地 LLM（LM Studio / Ollama 的 `http://127.0.0.1:端口/v1`）会触发 CORS 被拦截。项目已有同源代理方案，调用 LLM 时必须复用：

```typescript
// ❌ BAD：直接 fetch baseUrl，本地模型会 CORS 失败
await fetch(`${baseUrl}/chat/completions`, { ... })

// ✅ GOOD：统一走封装，自动判断是否经 /api/llm-proxy 转发
import { postChatCompletions } from './llmClient'
const res = await postChatCompletions(baseUrl, apiKey, body)
```

## 相关文件

- `src/services/llmClient.ts`：判断本地地址、拼代理路径、统一错误提示。
- `vite-plugin-llm-proxy.ts`：Vite dev/preview 中间件，按 `X-LLM-Base-URL` 头转发，仅允许本地目标。

## 注意

- 云端 API（OpenAI / DeepSeek 等）保持直连，不走代理。
- LLM 请求只发送资产汇总数字，仅在用户主动触发时调用。
- 修改 `vite-plugin-llm-proxy.ts` 或 `vite.config.ts` 后需重启 dev server。
